/**
 * Carousel library — public API.
 * Import from '@/components/carousel' instead of reaching into individual files,
 * so internal file renames never break consumers.
 */
export { default as CarouselDots } from "./CarouselDots";
